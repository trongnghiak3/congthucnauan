<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Đăng Nhập - KemzRecipe</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-yellow-50">
  <div class="min-h-screen flex items-center justify-center p-4">
    <!-- Container trắng bao quanh -->
    <div class="bg-white rounded-3xl shadow-2xl flex flex-col md:flex-row max-w-4xl w-full overflow-hidden">
      
      <!-- Hình ảnh bên trái -->
      <div class="hidden md:block md:w-1/2">
        <img 
          src="https://images.unsplash.com/photo-1600891964599-f61ba0e24092?auto=format&fit=crop&w=800&q=80" 
          alt="Hình ảnh món ăn" 
          class="w-full h-full object-cover"
        />
      </div>
      
      <!-- Form đăng nhập bên phải -->
      <div class="w-full md:w-1/2 p-10 flex flex-col justify-center">
        <h2 class="text-3xl font-bold text-yellow-600 mb-4 text-center">Chào mừng đến với KemzRecipe</h2>
        <p class="text-center text-gray-600 mb-8">Hãy đăng nhập để khám phá hàng ngàn công thức nấu ăn hấp dẫn!</p>
        
        <form action="/login" method="POST" class="space-y-6">
          <!-- Email -->
          <div>
            <label for="email" class="block text-gray-700 font-medium mb-2">Email</label>
            <input 
              type="email" 
              id="email" 
              name="email" 
              required 
              placeholder="you@example.com"
              class="w-full p-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-500"
            />
          </div>
          <!-- Mật khẩu -->
          <div>
            <label for="password" class="block text-gray-700 font-medium mb-2">Mật khẩu</label>
            <input 
              type="password" 
              id="password" 
              name="password" 
              required 
              placeholder="********"
              class="w-full p-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-500"
            />
          </div>
          <!-- Nút đăng nhập -->
          <div>
            <button 
              type="submit" 
              class="w-full bg-yellow-500 text-white py-3 rounded-md hover:bg-yellow-600 transition duration-200 font-semibold"
            >
              Đăng Nhập
            </button>
          </div>
          <!-- Liên kết hỗ trợ -->
          <div class="text-center">
            <a href="/forgot-password" class="text-sm text-yellow-600 hover:underline">Quên mật khẩu?</a>
          </div>
          <div class="text-center">
            <p class="text-sm text-gray-600">
              Bạn chưa có tài khoản?
              <a href="/register" class="text-yellow-600 hover:underline font-medium">Đăng ký ngay</a>
            </p>
          </div>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
